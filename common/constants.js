import values from 'lodash/values';

export const SourceType = {
  MISC: 'misc',
  RESEARCH: 'research',
  ARTICLE: 'article',
  AUTHORITY: 'authority',
};

export const SOURCE_TYPES = values(SourceType);
